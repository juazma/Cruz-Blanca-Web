import type { ReactNode } from "react";

export const metadata = {
  title: "Camareros · Cruz Blanca",
};

export default function CamarerosLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
